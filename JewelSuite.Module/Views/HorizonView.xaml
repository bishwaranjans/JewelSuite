<UserControl x:Class="JewelSuite.Module.Views.HorizonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
        <TextBlock Text="{Binding Title}" FontSize="18" Margin="5"/>
        <CheckBox IsChecked="{Binding CanShowVolume}" Content="Can Execute" Margin="5"/>
        <Button Command="{Binding ShowVolumeCommand}" Content="Show Volume" HorizontalAlignment="Left" MinWidth="250" Margin="5" />
        <TextBlock Text="{Binding UpdateText}"  Margin="5"/>
    </StackPanel>
</UserControl>
